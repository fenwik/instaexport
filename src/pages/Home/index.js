import React, { memo } from 'react';
import { Container } from 'react-grid-system';

const Home = () => (
  <div>
    <Container>
      Home page
    </Container>
  </div>
);

export default memo(Home);
